---
type: topic
tags:
  - 聊天机器人
  - Ollama
  - API
  - Python
domain: 模型部署
description: 使用 Python 调用 Ollama API 实现聊天机器人功能
created: 2025-05-01
updated: 2026-05-23
status: raw
---

# 聊天机器人

> 通过python调用API实现一系列功能

# 安装依赖包

本地需要先安装下依赖包

```python
pip install ollama
pip install langchain -i https://pypi.tuna.tsinghua.edu.cn/simple
pip install langchain-community -i https://pypi.tuna.tsinghua.edu.cn/simple
pip install dashscope -i https://pypi.tuna.tsinghua.edu.cn/simple
pip install streamlit==1.32.0 -i https://pypi.tuna.tsinghua.edu.cn/simple
```

# Ollama模块调用Qwen模型API

### 文本扩写

> 需求：通过python调用API实现基于qwen3在4b数据集下实现对文本的续写，用户输入一个简短的故事开头，系统能够生成一段连贯、有趣的续写内容。

![聊天对话接口-文字形式 - 信息提取](assets/聊天对话接口-文字形式%20-%20信息提取-20251213154438-fougru9.png)

```python
import ollama
# 调用ollama的聊天对话接口
response = ollama.chat(model='qwen3:4b',messages=[{'role': 'user', 'content': '从前有座山，山里有个庙，续写一下', },])
# 提取模型回复结果
print(response.message.content)        # 方式一
print(response['message']['content'])  # 方式二
```

### 文本问答

> 需求：构建一个简单的命令行交互程序，用户可以通过输入问题与AI模型进行对话。该程序将使用qwen3:4b语言模型。用户可以通过命令行输入问题，程序将调用AI模型生成回答，并将结果输出到终端。

```python
import ollama
while True:
    # 获取prompt提示词
    prompt = input("请输入您的问题：")
	# 调用模型，回答问题
    response = ollama.chat(model='qwen3:4b',
                           messages=[{'role': 'user', 'content': prompt, }, ])
    # 提取模型回复结果
    result = response['message']['content']
    print(result)
```

### 编程代码

> 需求：构建一个基于AI模型的代码生成工具，用户可以通过输入自然语言描述所需功能，AI模型将自动生成相应的Python代码。该工具旨在帮助开发者快速获取代码片段，减少手动编写代码的时间。

```python
import ollama
# 获取prompt提示词
prompt = """
请为以下功能生成一段Python代码：
求两个数的最大公约数
"""
# 调用模型，回答问题
response = ollama.chat(
    model='qwen3:4b',
    messages=[{'role': 'user', 'content': prompt}, ])
# 提取模型回复结果
result = response['message']['content']
print(result)
```

### 情感标签

> 需求：构建一个基于AI模型的用户反馈分类工具，用户可以通过输入具体的反馈内容，AI模型将自动将其归类到预设的类别中（如“价格过高”、“售后支持不足”、“产品使用体验不佳”、“其他”）。该工具旨在帮助企业快速分析用户反馈，识别主要问题，并采取相应的改进措施。

```python
import ollama
# 获取prompt提示词
prompt = """
你需要对用户的反馈进行原因分类。
分类包括：价格过高、售后支持不足、产品使用体验不佳、其他。
回答格式为：分类结果：xx。
用户的问题是：性价比不高，我觉得不值这个价钱。
"""
# 调用模型，回答问题
response = ollama.chat(model='qwen2:0.5b',
                       messages=[{'role': 'user', 'content': prompt}, ])
result = response['message']['content']
print(result)
```

# Streamlit

基于Streamlit实现前端聊天页面开发

> ​`Streamlit`​ 官方介绍：能在几分钟内把 `Python`​ 脚本变成可分享的网站。只需使用纯 `Python`​ ，无需前端经验。
>
> 官方网站：[https://streamlit.io/](https://streamlit.io/)

### Streamlit 安装

> 前提：已有 python 环境（或python虚拟环境）

```powershell
pip install streamlit==1.32.0 -i https://pypi.tuna.tsinghua.edu.cn/simple

conda install streamlit
```

```powershell
streamlit hello   # 运行测试
```

```python
  Welcome to Streamlit. Check out our demo in your browser.

  Local URL: http://localhost:8501
  Network URL: http://192.168.28.102:8501

  Ready to create your own Python apps super quickly?
  Head over to https://docs.streamlit.io

  May you create awesome apps!

```

### Streamlit 运行

```python
# 终端
streamlit run yourscript.py
```

### 基础语法

#### 导入 Streamlit 包

```python
import streamlit as st
```

#### 标题 title

使用 `st.title()`​ 可以设置标题内容

```python
st.title('Streamlit标题')
```

#### 段落 write

段落就是 `HTML`​ 里的 `<p>`​ 元素，在 `streamlit`​ 里使用 `st.write('内容')`​ 的方式去书写。

```python
st.write('Hello')
```

#### 使用 markdown

​`streamlit`​ 是支持使用 `markdown`​ 语法来写页面内容的，只需使用单引号或者双引号的方式将内容包起来，并且使用 `markdown`​ 的语法进行书写，页面就会出现对应样式的内容。

```python
"# 1级标题"
"## 2级标题"
"### 3级标题"
"#### 4级标题"
"##### 5级标题"
"###### 6级标题"
```

#### 图片 image

渲染图片可以使用 `st.image()`​ 方法，也可以使用 `markdown`​ 的语法。

```python
st.image(图片地址, [图片宽度])   # 图片宽度不是必填项（width调整宽高比）
st.image('./avatar.jpg', width=400)
```

#### 表格 table

​`streamlit`​ 有静态表格和可交互表格。表格在数据分析里属于常用组件，所以 `streamlit`​ 的表格也支持 `pandas`​ 的 `DataFrame`​ 。

###### 静态表格 table

静态表格使用 `st.table()`​ 渲染，出来的效果就是 `HTML`​ 的 `<table>`​。

​`st.table()`​ 支持传入字典、`pandas.DataFrame`​ 等数据。

```python
import streamlit as st
import pandas as pd

st.write('dict字典形式的静态表格')
st.table(data={
    'name': ['张三', '李四', '王五'],
    'age': [18, 20, 22],
    'gender': ['男', '女', '男']
})

st.write('pandas中dataframe形式的静态表格')

df = pd.DataFrame(
    {
        'name': ['张三', '李四', '王五'],
        'age': [18, 20, 22],
        'gender': ['男', '女', '男']
    }
)
st.table(df)
```

###### 可交互表格 dataframe

可交互表格使用 `st.dataframe()`​ 方法创建，和 `st.table()`​ 不同，`st.dataframe()`​ 创建出来的表格支持按列排序、搜索、导出等功能。

```python
import streamlit as st
import pandas as pd

st.write('dict字典形式的可交互表格')
st.dataframe(data={
    'name': ['张三', '李四', '王五'],
    'age': [18, 20, 22],
    'gender': ['男', '女', '男']
})

st.write('pandas中dataframe形式的可交互表格')
df = pd.DataFrame(
    {
        'name': ['张三', '李四', '王五'],
        'age': [18, 20, 22],
        'gender': ['男', '女', '男']
    }
)
st.dataframe(df)
```

#### 分割线 divider

分隔线就是 `HTML`​ 里的 `<hr>`​ 。在 `streamlit`​ 里使用 `st.divider()`​ 方法绘制分隔线。

```python
st.divider()
```

#### 输入框 text_input

知道怎么声明变量后，可以使用一个变量接收输入框的内容。

输入框又可以设置不同的类型，比如普通的文本输入框、密码输入框。

- 基本输入框

  输入框使用 `st.text_input()`​ 渲染。

  ```python
  name = st.text_input('请输入你的名字：')
  if name:
    st.write(f'你好，{name}')
  ```

- 密码框

  如果要使用密码框，可以给 `st.text_input()`​ 加多个类型 `type="password"`​。

  ```python
  pwd = st.text_input('密码是多少？', type='password')
  ```

- 数字输入框

  数字输入框需要使用 `number_input`​

  ```python
  age = st.number_input('年龄：')
  st.write(f'你输入的年龄是{age}岁')
  ```

  众所周知，正常表达年龄是不带小数位的，所以我们可以设置 `st.number_input()`​ 的步长为1，参数名叫 `step`​。

  ```python
  st.number_input('年龄：', step=1)
  ```

  这个步长可以根据你的需求来设置，设置完后，输入框右侧的加减号每点击一次就根据你设置的步长相应的增加或者减少。

  还有一点，人年龄不可能是负数，通常也不会大于200。可以通过 `min_value`​ 和 `max_value`​ 设置最小值和最大值。同时还可以通过 `value`​ 设置默认值。

  ```python
  st.number_input('年龄：', value=20, min_value=0, max_value=200, step=1)
  ```

#### 多行文本框 text_area

创建多行文本框使用的是 `st.text_area()`​，用法和 `st.text_input()`​ 差不多。

```python
paragraph = st.text_area("多行内容：")
```

#### Chat Elements

- Chat input 文本输入框

  ```python
  prompt = st.chat_input("Say something")
  if prompt:
      st.write(f"User has sent the following prompt: {prompt}")
  ```
- Chat Message 聊天信息框

  ```python
  # 使用 st.chat_message 创建一个用户消息容器，用于显示用户的消息
  # 'user' 表示这是用户发送的消息
  with st.chat_message('user'):
      # 在用户消息容器中显示文本 'Hello '
      st.write('Hello ')

  # 使用 st.chat_message 创建一个消息容器，用于显示回复消息
  message = st.chat_message('assistant')
  # 在消息容器中显示文本 'Hello Human'，模拟助手的回复
  message.write('Hello Human')
  ```

- Chat

  ```python
  # 导入 Streamlit 库，Streamlit 是一个用于快速创建数据应用的 Python 库
  import streamlit as st

  # 使用 st.chat_input 创建一个聊天输入框，提示用户输入问题
  prompt = st.chat_input('请输入您的问题: ')

  st.write(f'您的问题是: {prompt}')

  # 使用 st.chat_message 创建一个用户消息容器，用于显示用户的消息
  # 'user' 表示这是用户发送的消息
  with st.chat_message('user'):
      # 在用户消息容器中显示文本 'Hello '
      st.write('Hello ')

  # 使用 st.chat_message 创建一个消息容器，用于显示回复消息
  message = st.chat_message('assistant')
  # 在消息容器中显示文本 'Hello Human'，模拟助手的回复
  message.write('Hello Human')
  ```

# 基于Ollama和Qwen的聊天机器人

> 目的：实现基于Python调用Qwen模型API接口聊天机器人开发
>
> 需求：构建一个基于大模型的本地智能聊天机器人，利用其强大的自然语言处理和生成能力，为用户提供高效、精准、个性化的对话服务。该聊天机器人将集成先进的大规模预训练语言模型（如GPT、Qwen等），具备自然语言理解、多轮对话、情感分析、知识问答等核心功能，并可根据具体应用场景进行定制化扩展，例如客服咨询、教育辅导、娱乐互动等。
>
> 项目采用模块化设计，前端通过Streamlit等框架实现简洁易用的交互界面，后端基于Ollama等平台进行模型部署和管理，确保系统的高效性和可扩展性。项目目标是打造一个智能化、人性化的聊天机器人，提升用户体验，降低人工成本，并探索大模型技术在不同领域的创新应用。

### 模型调用

```python
# 1. 导包
import ollama

# 2.向大语言模型发送请求, 并获取其返回结果
def get_response(prompt):
    # 2.1 发起请求, 获取响应信息
    # response = ollama.chat(
    #     model='qwen3:4b',
    #     messages=[{'role': 'user', 'content': prompt}]
    # )
    # 前端传过来的问题, 是[列表 + 字典]的形式, 已符合Ollama API的格式
    response = ollama.chat(model='qwen3:4b', messages=prompt[-50:])
    # 2.2 返回模型处理后的结果(即: 聊天机器人的回答)
    return response['message']['content']

# 3. 测试
if __name__ == '__main__':
    prompt = input('请给聊天机器人发送消息:')
    response = get_response(prompt)
    print(response)
```

### 前端实现

```python
# 1. 导入相关包
# streamlit库: Python代码实现搭建前端页面 并 部署
import streamlit as st
# langchain库: 聊天机器人核心模块, ConversationBufferMemory: 聊天记录存储器(存储所有聊天信息的)
from langchain_classic.memory import ConversationBufferMemory
# 自定义模块: 封装了模型处理函数, 获取模型处理结果
from chat_utils import get_response

# 2. 主界面主标题
st.title("智能聊天机器人")

# 3. 判断是否有 历史消息记录对象, 如果没有就创建, 并存储所有的消息记录
if "memory" not in st.session_state:  # session_state: 存储会话状态数据的字典, 用于存储会话数据
    # 3.1 创建 ConversationBufferMemory 对象, 并存储到 session_state 中
    st.session_state.memory = ConversationBufferMemory()
    # 3.2 添加 机器人欢迎语
    st.session_state.messages = [{'role': 'assistant', 'content': '你好，我是智能聊天机器人，有什么可以帮助你的么？'}]

# 4. 显示所有消息记录（遍历 session_state['messages']）
for message in st.session_state['messages']:
    # 4.1 创建消息容器
    with st.chat_message(message['role']):
        # 4.2 显示当前角色内容
        st.markdown(message['content'])

# 5. 聊天窗口（接收用户输入的内容）
prompt = st.chat_input("请给聊天机器人发送消息")
# 6. 如果文本框有数据时
if prompt:
    # 6.1 将用户消息添加到 session_state['messages'] 中
    st.session_state.messages.append({'role': 'user', 'content': prompt})
    # 6.2 显示用户消息
    st.chat_message("user").markdown(prompt)
    # 6.3. 向模型发起请求，并获取返回结果
    with st.spinner('智能聊天机器人正在思考中...'):  # 加入延时等待提示组件
        # 调用自定义的 chat_util模块中的 get_response()函数, 获取模型的处理结果
        response = get_response(st.session_state.messages)
    # 6.4 把模型的处理结果添加到 历史消息记录中, 并展示到前端页面.
    st.session_state.messages.append({'role': 'assistant', 'content': response})
    st.chat_message('assistant').markdown(response)
```

```python
streamlit run yourscript.py
```
