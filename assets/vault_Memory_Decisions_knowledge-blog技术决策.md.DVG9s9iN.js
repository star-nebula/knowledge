import{_ as a,a8 as o,o as l,c as r,ah as g,a as t,a3 as d,K as c,L as p}from"./chunks/framework.D31WLDJp.js";import"./chunks/theme.CP9whvzN.js";const w=JSON.parse('{"title":"个人知识库博客 — 技术决策记录","description":"","frontmatter":{"type":"decision","scope":"knowledge-blog","created":"2026-09-12T00:00:00.000Z","updated":"2026-09-12T00:00:00.000Z","tags":["VitePress","决策","首页","双向链接","发布机制"]},"headers":[],"relativePath":"vault/Memory/Decisions/knowledge-blog技术决策.md","filePath":"vault/Memory/Decisions/knowledge-blog技术决策.md","lastUpdated":1789374053000}'),u={name:"vault/Memory/Decisions/knowledge-blog技术决策.md"};function m(b,e,f,_,x,h){const n=o("VPNolebaseInlineLinkPreview"),s=o("NolebaseGitContributors"),i=o("NolebaseGitChangelog");return l(),r("div",null,[e[2]||(e[2]=g('<h1 id="个人知识库博客-—-技术决策记录" tabindex="-1">个人知识库博客 — 技术决策记录 <a class="header-anchor" href="#个人知识库博客-—-技术决策记录" aria-label="Permalink to “个人知识库博客 — 技术决策记录”">​</a></h1><p>提炼自项目长期记忆（2026-07 整月）。按时间倒序。</p><ul><li><p><strong>2026-07-19 · Knowledge 全量接入站点 + CI 安全化</strong>：<code>.gitignore</code> 明确 <code>vault/Knowledge/</code> 为私人库、&quot;不发布到网站&quot;（发布走 <code>status: published</code> 选择性开关）。<code>config.ts</code> 用 <code>HAS_KNOWLEDGE = existsSync(resolve(process.cwd(),&#39;vault/Knowledge&#39;))</code> 条件式接入——本地（目录存在）全量渲染 227 篇 Knowledge / 共 323 页；CI 干净 checkout（目录被 gitignore 不存在）自动跳过，<strong>同一份 config 本地与 CI 双绿</strong>。<code>scripts/build.mjs</code> 固定为按文件系统路径解析 VitePress CLI 入口 + <code>spawnSync(process.execPath,[bin,&#39;build&#39;],{NODE_OPTIONS:--max-old-space-size=8192})</code>，规避 Windows 下 <code>spawnSync(&#39;vitepress&#39;)</code> ENOENT 与 vitepress <code>exports</code> 屏蔽 <code>./bin/vitepress.js</code> 两个坑。⚠️ 部署前置：<code>config.ts</code> import 的 <code>scripts/knowledge-org.ts</code> / <code>scripts/build.mjs</code> / <code>sanitize-wikilink-percent.ts</code> 当前未 git 跟踪，CI 干净 checkout 会构建挂——必须提交这三者（+ 已改的 config.ts/package.json/vite.config.ts）GitHub Pages 才能绿。</p></li><li><p><strong>2026-07-19 · vault/Knowledge 基于 category 重组（数据层完成，站点集成待决策）</strong>：已注入 202 篇 <code>category</code> frontmatter、生成/复用 27 个 MOC、写好 <code>buildKnowledgeSidebar()</code> 动态多级侧边栏（已验证树正确）。实测把 Knowledge 全量渲染进站点会触发 4 类既有内容缺陷（裸 <code>%</code> 文件名/链接→decodeURI 崩、缺失媒体 <code>![[assets/x]]</code>→Rollup hard-fail、原始 HTML/表格→Vue SFC &quot;missing end tag&quot;、误写 <code>[[PPO](url)]</code> 错链）——正是当初 <code>.gitignore</code>+<code>srcExclude</code> 排除它的原因。已加 3 道渲染期兜底（不改动源文件、对既有发布内容无害）：<code>sanitize-wikilink-percent.ts</code> + config <code>link_open</code> href 兜底 + <code>vite.config.ts</code> <code>missingAssetStub</code>。待决策 Knowledge 与站点关系（维持不进站点 / 全量清理后接入 / 仅渲染干净子集）。</p></li><li><p><strong>2026-07-16 · 发布同步机制（单一数据源）已撤销</strong>：曾试 <code>Knowledge/Projects</code> → <code>vault/笔记</code> 自动派生（scripts/sync-published-notes.mjs + publish_category 标记 + pre-commit 同步）。用户认为发布内容应独立于个人笔记、可能经改写，故<strong>撤回</strong>：删脚本、还原 package.json、撤 pre-commit 同步（保留 assets sync + 100MB 护栏）、删除 manifest、<code>vault/笔记/...</code> 还原为原手写版。<strong>结论：<code>vault/笔记</code> 视为独立手写/手管内容，不做自动派生。</strong></p></li><li><p><strong>2026-07-14 · 接入 Nolebase 双向链接</strong>：<code>@nolebase/markdown-it-bi-directional-links</code>（导出名 <code>BiDirectionalLinks</code>）。<strong>重要限制</strong>：<code>[[ ]]</code> 语法在 <strong>Markdown 表格单元格内不生效</strong>（插件不处理 table cell inline 内容，HTML 会漏出字面 <code>[[...]]</code>；且别名分隔符 <code>|</code> 与表格列分隔符 <code>|</code> 冲突会撑破表格）。<strong>结论：表格内的链接一律用原生 markdown <code>[label](./file.md)</code>，不要写 <code>[[ ]]</code>。</strong> 其余 prose 内的 <code>[[file|label]]</code> 正常渲染。</p></li><li><p><strong>2026-07-05 · 首页日间模式支持</strong>：Canvas 遮罩色从硬编码 <code>#161618</code> 改为主题感知（<code>isDarkMode()</code> → <code>#161618</code>/<code>#ffffff</code>），墨水染色同理（黑/白）。CSS 定义 <code>--home-bg</code> 变量，所有 8 处硬编码 <code>#161618</code> 替换为 <code>var(--home-bg)</code>。新增 <code>MutationObserver</code> 监听 <code>&lt;html&gt;</code> class 变化，主题切换时自动重新渲染文字图层。</p></li><li><p><strong>2026-07-05 · 文字染色最终方案</strong>：弃用 CSS <code>mix-blend-mode: multiply</code>（透明区域产生黑块），改用 Canvas <code>globalCompositeOperation = &#39;source-atop&#39;</code>。在 <code>.text</code>/<code>.tagline</code> 上覆盖 canvas，先绘制文字像素（遮罩），再 source-atop 合成黑色墨滴（数学保证 <code>αo=αb</code>，非文字区域永远透明）。离屏 canvas 缓存预渲染文字避免每帧字体光栅化。隐藏 DOM 原文字由 canvas 替代。</p></li><li><p><strong>2026-07-05 · 文字渲染逐字符修复</strong>：行分组+<code>fillText(line.text)</code> 在 <code>.text</code>（有 <code>letter-spacing: -0.4px</code> 且会换行）上偏差明显。改为逐字符渲染：<code>measureTextChars</code> 返回每个字符 <code>{char,x,y}</code>，<code>renderTextToOffscreen</code> 对每个字符单独 <code>fillText</code>，位置 = DOM 精确像素位置，彻底消除 letter-spacing/断行/字体度量差异。</p></li><li><p><strong>2026-07-05 · 文字位置偏移修复</strong>：<code>range.getBoundingClientRect().top</code> 返回 line box 顶部，canvas <code>textBaseline=&#39;top&#39;</code> 对齐 em square 顶部，两者差 half-leading = <code>(lineHeight - fontSize) / 2</code>。修复：<code>fillText(ch.char, ch.x, ch.y + leadingOffset)</code>。<code>.tagline</code> 偏移 6px（lineH=36,fontSz=24）。</p></li><li><p><strong>2026-07-05 · 文字大小修复（Range API）</strong>：canvas 文字和 DOM 文字大小不一致的根因是 <code>wrapTextCJK</code> + <code>measureText</code> 重新断行与浏览器实际断行不匹配。改用 <strong>Range API</strong>（<code>document.createTreeWalker</code> + <code>range.getBoundingClientRect()</code>）直接读取每个字符的 DOM 真实坐标，按 y 分组成行后在 canvas 逐行绘制。不再设 <code>ctx.letterSpacing</code>。<code>onMounted</code> 加 <code>document.fonts.ready</code> 确保字体加载后再测量。</p></li><li><p><strong>2026-07-05 · 文字染色效果修复（isolation）</strong>：染色 canvas 从注入 <code>.container</code>（覆盖全部 Hero 内容）改为分别注入 <code>.text</code> 和 <code>.tagline</code> 元素内部，<code>isolation: isolate</code> 也移到这两个元素上。这样 <code>mix-blend-mode: multiply</code> 只在各自文字元素内部生效：白字染黑、透明背景无黑块，且不影响 h1/logo/按钮。</p></li><li><p><strong>2026-07-05 · 首页聚光灯改为墨水扩散系统</strong>：鼠标移动沿途生成墨滴，每个墨滴用 ease-out-cubic 向外洇开并淡出，模拟墨水写到纸上的扩散效果。灵感来自 mimo.xiaomi.com。用户已手动调定参数：SPAWN_DIST=14, DROP_INIT_R=15, DROP_MAX_R=100, DROP_LIFESPAN=100, DROP_OPACITY=1.0，<strong>后续优化不得改动这些值</strong>。</p></li></ul><h2 id="关联" tabindex="-1">关联 <a class="header-anchor" href="#关联" aria-label="Permalink to “关联”">​</a></h2>',4)),t("ul",null,[t("li",null,[e[1]||(e[1]=d("项目概况见 ",-1)),c(n,{class:"route-link nolebase-route-link-invalid",href:"#",target:"_target"},{default:p(()=>[...e[0]||(e[0]=[d("knowledge-blog",-1)])]),_:1})])]),c(s),c(i)])}const P=a(u,[["render",m]]);export{w as __pageData,P as default};
